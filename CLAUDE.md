# PJ Arts — project rules

## Tooling
- Do NOT use the Playwright MCP plugin (browser automation/testing) unless explicitly asked.
  For verifying the running app in a browser, use the Chrome DevTools MCP instead.
